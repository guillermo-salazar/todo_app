<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @list.title %>
</p>

<p>
  <strong>Tasks (<%= @list.tasks.count %>):</strong>
  <ul>
  <% @list.tasks.each do |task| %>
   <li><%=task.description %></li>
  <% end %>
  </ul>
</p>

<%= link_to 'Edit', edit_list_path(@list) %> |
<%= link_to 'Back', lists_path %>
